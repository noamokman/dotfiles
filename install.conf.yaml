- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.zshrc:
      force: true
      path: zshrc
    ~/.zsh:
    ~/.npmrc:
    ~/.oh-my-zsh:

- shell:
  - ["git submodule update --init --recursive", Installing submodules]
