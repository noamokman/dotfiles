- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- shell:
  - ["git submodule update --init --recursive", Installing submodules]

- link:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.zshrc:
      force: true
      path: zshrc
    ~/.zsh:
    ~/.npmrc:
    ~/.oh-my-zsh:
      force: true
      path: oh-my-zsh