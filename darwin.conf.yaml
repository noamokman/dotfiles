- brew:
    - curl
    - git
    - zsh
    - nodejs

- cask:
    - docker
    - google-chrome
    - iterm2
    - vlc

- shell:
  - ['[[ "$SHELL" != */zsh ]] && chsh -s $(which zsh)', Making zsh the default shell]