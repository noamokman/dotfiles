- shell:
    - [
        'if [[ "$SHELL" != */zsh ]]; then chsh -s $(which zsh); fi',
        Making zsh the default shell,
      ]
    - ["npm i -g uuid npm-check-updates np", Installing global node modules]
