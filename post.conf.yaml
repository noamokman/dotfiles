- shell:
    - [
        'if [[ "$SHELL" != */zsh ]]; then chsh -s $(which zsh); fi',
        Making zsh the default shell,
      ]
    - [
        'yarn global add uuid npm-check-updates clipboard-cli',
        Installing global node modules,
      ]
